spring.application.name=notification-service

server.port=9004

# DB
spring.datasource.url=jdbc:mysql://localhost:3306/hms_notification_db
spring.datasource.username=root
spring.datasource.password=Ish983556

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Eureka
eureka.client.service-url.defaultZone=http://localhost:8761/eureka

# Actuator
management.endpoints.web.exposure.include=health,info

# Kafka Consumer
spring.kafka.bootstrap-servers=localhost:9092
spring.kafka.consumer.group-id=notification-group
spring.kafka.consumer.auto-offset-reset=earliest
spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.consumer.value-deserializer=org.springframework.kafka.support.serializer.ErrorHandlingDeserializer
spring.kafka.consumer.properties.spring.deserializer.value.delegate.class=org.springframework.kafka.support.serializer.JsonDeserializer
spring.kafka.consumer.properties.spring.json.trusted.packages=*
spring.kafka.consumer.properties.spring.json.use.type.headers=false
spring.kafka.consumer.properties.spring.json.value.default.type=java.util.Map

# Email Configuration (using Gmail SMTP - update with your credentials)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=ishughhhhh02@gmail.com
spring.mail.password=uvrd rlau lghm xtoo
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# Email sender details
notification.email.from=ishughhhhh02@gmail.com
spring.mail.properties.mail.debug=true
notification.email.from-name=Hotel Booking System

# SMS Configuration (mock for now - integrate with actual SMS provider later)
notification.sms.enabled=false
notification.sms.provider=mock

# Feedback Configuration
notification.feedback.base-url=http://localhost:3000/feedback

